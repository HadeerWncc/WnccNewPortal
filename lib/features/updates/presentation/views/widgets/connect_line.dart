import 'package:flutter/material.dart';

class ConnectLine extends StatelessWidget {
  const ConnectLine({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Container(
        width: 1.5,
        color: Colors.white,
      ),
    );
  }
}
